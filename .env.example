# Production site URL (canonical, sitemap, Open Graph, JSON-LD)
NEXT_PUBLIC_SITE_URL=https://arbazarif.com

# Playwright (optional — defaults to http://127.0.0.1:3010)
# PLAYWRIGHT_BASE_URL=http://127.0.0.1:3010

# Local dev port (default 3010 — avoids CBS/PropEx apps on :3000)
# PORT=3010
